<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GestionEnvios" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GestionEnvios" parent="Base.Theme.GestionEnvios" />

    <style name="BotonEnviar">
        <item name="android:layout_marginTop">120dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">Enviar</item>
        <item name="android:layout_marginStart">80dp</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
    </style>

    <style name="BotonCancelar">
        <item name="android:layout_marginTop">120dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">Cancelar</item>
        <item name="android:layout_marginStart">60dp</item>
    </style>
</resources>