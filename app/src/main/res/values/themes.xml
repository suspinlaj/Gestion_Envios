<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GestionEnvios" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GestionEnvios" parent="Base.Theme.GestionEnvios" />

    <!-- EDIT TEXT -->
    <style name="EditTextsBase">
        <item name="android:layout_width">331dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="fontFamily">@font/titulo1</item>
        <item name="android:textSize">25sp</item>
        <item name="android:ems">10</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="EditTextsBase.EditTexts">
        <item name="android:layout_marginTop">60dp</item>
    </style>

    <!-- BOTONES -->
    <style name="BotonBase" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/marron</item>
        <item name="strokeWidth">4dp</item>
        <item name="strokeColor">@color/marron</item>
        <item name="android:textSize">21sp</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:layout_width">140dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">45dp</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="fontFamily">@font/titulo2</item>

    </style>
    <!-- botón enviar -->
    <style name="BotonBase.Enviar">
        <item name="android:text">Enviar</item>
    </style>
    <!-- botón cancelar -->
    <style name="BotonBase.Cancelar">
        <item name="android:text">Cancelar</item>
    </style>

    <!-- Botón Atrás -->
    <style name="ImgAtras">
        <item name="android:layout_width">35dp</item>
        <item name="android:layout_height">35dp</item>
        <item name="android:layout_marginTop">25dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:layout_marginEnd">15dp</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="srcCompat">@drawable/flechaa</item>
    </style>

    <!-- Titulos Pantallas -->
    <style name="TitulosPantallas">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">28dp</item>
        <item name="fontFamily">@font/titulo2</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">25dp</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
    </style>

</resources>